import { FooterComponent } from "../components";

const Footer = () => {
    return (
        <div className="w-full bg-gray-100 flex flex-col justify-start items-center mt-10 p-10 rounded-2xl shadow-2xl">
            <FooterComponent />
        </div>
    );
};

export default Footer;
